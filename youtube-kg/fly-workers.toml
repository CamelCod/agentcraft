# Fly.io configuration for Celery Workers
app = "youtube-kg-workers"
primary_region = "lhr"

[build]
  dockerfile = "Dockerfile.worker"

[env]
  APP_ENV = "production"
  LLM_PROVIDER = "anthropic"

[deploy]
  strategy = "rolling"

[[vm]]
  cpu_kind = "shared"
  cpus = 2
  memory_mb = 1024
